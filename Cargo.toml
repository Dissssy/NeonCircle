[workspace]
members = ["neon_circles", "common", "config-command", "music-commands", "lts", "national-debt"]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
